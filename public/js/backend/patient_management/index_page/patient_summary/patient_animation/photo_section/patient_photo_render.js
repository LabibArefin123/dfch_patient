(function () {
    function render(patient) {
        const html = `
        <!-- Paste your entire HTML template here -->
    `;

        $("#patientAnimationPhotoContainer").html(html);
    }

    window.PatientPhotoAnimate.render = render;
})();
